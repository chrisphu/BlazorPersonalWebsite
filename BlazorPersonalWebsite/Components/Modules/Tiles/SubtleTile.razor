<a href="@(Href)" class="rounded-[0.75rem] border-2 border-dashed border-slate-300 bg-slate-50 p-4 pb-2 text-left
                         text-slate-400 transition-all duration-75 ease-in-out hover:bg-slate-100 hover:text-black">
    <div class="mb-2 flex flex-nowrap gap-1 font-bold">
        <p class="self-center">
            @(TitlePrefix)
        </p>
        <p class="self-center">
            @(Title)
        </p>
    </div>
    <p class="mb-2 text-sm">
        @(Body)
    </p>
    <div class="flex flex-nowrap justify-end gap-1">
        @(ChildContent)
    </div>
</a>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public string TitlePrefix { get; set; } = string.Empty;
    [Parameter] public string Title { get; set; } = string.Empty;
    [Parameter] public string Body { get; set; } = string.Empty;
    [Parameter] public string Href { get; set; } = "/";
}